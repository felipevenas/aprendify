/**
 * Navbar foi substituída pela combinação AppSidebar + AppHeader (ver AppLayout).
 * Mantemos este componente como stub para preservar imports existentes nas páginas.
 */
const Navbar = () => null;

export default Navbar;
